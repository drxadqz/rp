extends: c3_farnet_s138_dual_film_texture_roughness_screen_20260716.yaml

output_dir: E:/perception_outputs/rscd_surface_classification/s138_dual_film_texture_roughness_full_20260716

# Full protocol for S138. Launch only if S137 fails and S138 screen passes both
# the S96 cap250 baseline and its same-budget off-control.

train:
  resume_from: E:/perception_outputs/rscd_surface_classification/s138_dual_film_texture_roughness_screen_20260716/best_checkpoint.pth
  evaluate_initial: true
  batch_size: 16
  grad_accum_steps: 1
  num_workers: 2
  prefetch_factor: 2
  lr: 0.0001
  weight_decay: 0.01
  epochs: 10
  amp: true
  augmentation: true
  balanced_sampling: true
  max_train_samples_per_class:
  samples_per_epoch: 0
  grad_clip_norm: 5.0
  log_every_steps: 2000

eval:
  batch_size: 32
  max_val_samples_per_class:
  max_test_samples_per_class:
